# Default Recent Blog Posts Variant

Three-column responsive grid of the most recent posts from a chosen Blog Index page. Post count is configurable via the block's `count` field (default: 3). Posts are fetched at render time via the `get_recent_blog_posts` template tag.
