"""API endpoints for the ``phoxtail.streams`` app.

Versions live in sub-packages (``v1``, ``v2``, ...). Each version exports
a ``router`` registered on the root ``NinjaAPI`` instance in
``phoxtail.api``.
"""
