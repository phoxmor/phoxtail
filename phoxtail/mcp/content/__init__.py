"""Core pages MCP tools.

Submodules (``pages``, ``body``, ``media``, ``resources``) are imported
explicitly by :mod:`phoxtail.mcp._register_core_tools` at server startup.
This package init is intentionally empty to avoid double-importing them.
"""
