The studio MCP tools for blocks, categories, collections, variants and shared
blocks name the codename their endpoint names, so a token scoped to less than
an act is not offered the tool that performs it. `phoxtail_studio_diff_variant`
names `view_blockvariant`: it reads a variant and compares it locally, writing
nothing.
