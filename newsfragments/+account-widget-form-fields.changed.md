Every remaining account page — change/set/reset password, reauthenticate,
sign-in code, email verification code and the email address forms — now renders
its fields through the shared MD3 widget set, so labels, errors and help text
look the same everywhere. Cancel on the confirm-code pages is a proper
secondary button instead of an inline-styled link.
