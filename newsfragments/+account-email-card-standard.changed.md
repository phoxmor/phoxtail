The email address management pages now use the same glass card, pill buttons
and form controls as the rest of the account pages, instead of their own
opaque white card. Submit buttons can carry a `name` and a `secondary` or
`danger` variant.
