The users app now declares its API and MCP tools the same way every other
phoxtail app does. `/api/users/v1/` and the `phoxtail_users_*` tools are
unchanged; the surface is found rather than wired by hand.
