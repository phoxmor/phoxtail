`phoxtail.core.test_settings` is a supported module: Django settings configured
the way phoxtail configures them, for packages that add an app to phoxtail and
need a test environment. Import it and append your app to `INSTALLED_APPS`.
