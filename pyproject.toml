[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "phoxtail"
version = "0.1.1"
description = "The engine behind every Phoxtail project."
readme = "README.md"
license = "BSD-3-Clause"
requires-python = ">=3.11"
authors = [{ name = "Phoxmor", email = "info@phoxtail.com" }]
maintainers = [{ name = "Evangelos Pisinas", email = "evangelos.pisinas@phoxtail.com" }]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Framework :: Django",
    "Framework :: Wagtail",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Build Tools",
]
dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
    "questionary>=2.0.0",
    "jinja2>=3.1.0",
    "httpx>=0.27.0",
]

[project.optional-dependencies]
docs = [
    "zensical>=0.0.28",
]
engine = [
    "django>=4.2",
    "wagtail>=6.0",
    "django-allauth>=0.60",
    "django-htmx>=1.17.0",
    "celery>=5.3",
    "wagtail-color-panel>=0.4.0",
    "pyyaml>=6.0",
    "wagtailmedia>=0.15",
    "sorl-thumbnail>=12.10",
]
blog = [
    "phoxtail[engine]",
]
dashboard = [
    "phoxtail[engine]",
]
booking = [
    "phoxtail[engine,dashboard]",
    "django-countries>=7.0",
    "django-phonenumber-field[phonenumbers]>=7.0",
    "django-timezone-field>=6.0",
    "django-celery-beat>=2.5",
    "django-filter>=23.0",
    "Faker>=19.0",
    "factory-boy>=3.3",
]
dev = [
    "pip-tools>=7.0.0",
    "pytest>=8.0.0",
    "pytest-django>=4.8.0",
    "pytest-httpx>=0.30.0",
    "pytest-cov>=5.0.0",
    "ruff>=0.4.0",
    "djhtml>=3.0.0",
    "freezegun>=1.2",
    "ipython>=8.0",
    "phoxtail[engine,blog,booking,dashboard,docs]",
]

[project.urls]
Homepage = "https://phoxtail.com"
Repository = "https://github.com/phoxmor/phoxtail"

[project.scripts]
phoxtail = "phoxtail.__main__:app"

[tool.setuptools.packages.find]
include = ["phoxtail*"]
exclude = [
    "phoxtail.cli.tests*",
]

[tool.setuptools.package-data]
"phoxtail" = ["project_template/**/*", "project_template/**/.*", "docs/**/*"]
"phoxtail.cli" = ["templates/**/*"]
"phoxtail.users" = ["templates/**/*", "static/**/*"]
"phoxtail.core" = ["templates/**/*", "static/**/*"]
"phoxtail.design" = ["management/data/**/*"]
"phoxtail.streams" = ["templates/**/*", "static/**/*", "management/data/**/*"]
"phoxtail.blog" = ["templates/**/*", "management/data/**/*"]
"phoxtail.dashboard" = ["templates/**/*", "static/**/*"]
"phoxtail.booking.core" = ["templates/**/*", "static/**/*"]
"phoxtail.booking.events" = ["templates/**/*", "static/**/*", "management/data/**/*"]
"phoxtail.booking.services" = ["templates/**/*", "static/**/*"]
"phoxtail.booking.subscriptions" = ["templates/**/*", "static/**/*"]
"phoxtail.booking.reservations" = ["templates/**/*", "static/**/*"]

[tool.pytest.ini_options]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "-v --tb=short"
testpaths = ["phoxtail/cli/tests", "phoxtail/core/tests", "phoxtail/dashboard/tests", "phoxtail/design/tests", "phoxtail/streams/tests", "phoxtail/booking/core/tests", "phoxtail/booking/events/tests", "phoxtail/booking/subscriptions/tests", "phoxtail/booking/reservations/tests"]
DJANGO_SETTINGS_MODULE = "phoxtail.core.tests.settings"

[tool.ruff]
line-length = 88
target-version = "py311"
extend-exclude = ["*/migrations/*"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
